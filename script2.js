const fs = require('fs'); let c = fs.readFileSync('src/lib/ai-tools.ts', 'utf8'); let ask = c.substring(c.indexOf('askForConfirmation:'), c.indexOf('lookupInvoices: tool({')); c = c.replace(ask, ''); c = c.replace('export function getAITools(supabase: any) {', 'export function getNativeAITools() {\n  return {\n    ' + ask + '  };\n}\n\nexport function getMCPToolsConfig(supabase: any) {\n  const mcpTool = (c: any) => c;\n  return {'); c = c.replace(/\btool\(\{/g, 'mcpTool({'); fs.writeFileSync('src/lib/mcp-tools.ts', c);
